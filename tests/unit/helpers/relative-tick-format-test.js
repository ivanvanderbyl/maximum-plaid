import { relativeTickFormat } from 'dummy/helpers/relative-tick-format';
import { module, test } from 'qunit';

module('Unit | Helper | relative tick format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = relativeTickFormat([42]);
  assert.ok(result);
});
