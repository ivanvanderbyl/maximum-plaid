import animationUtils from 'dummy/utils/animation-utils';
import { module, test } from 'qunit';

module('Unit | Utility | animation utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animationUtils();
  assert.ok(result);
});
